---
import { getIcon } from "@utils/ui";
import type { IconName } from "@utils/ui";

interface Props {
  name: IconName;
  className?: string;
}

const { name, className = "w-4 h-4" } = Astro.props;
---

<Fragment set:html={getIcon(name, className)} />
