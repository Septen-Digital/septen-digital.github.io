{
  "name": "septen",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "npm run build && wrangler dev",
    "optimize:greenfield-images": "node scripts/optimize-greenfield-images.mjs",
    "astro": "astro",
    "generate-types": "wrangler types",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^14.1.5",
    "@astrojs/preact": "^6.0.1",
    "@astrojs/sitemap": "^3.7.3",
    "@tailwindcss/vite": "^4.3.2",
    "astro": "^7.0.6",
    "preact": "^10.29.4"
  },
  "devDependencies": {
    "tailwindcss": "^4.3.2",
    "typescript": "^5.9.2",
    "wrangler": "^4.114.0"
  }
}
